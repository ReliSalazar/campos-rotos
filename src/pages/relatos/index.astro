---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { relatos } from "../../data/relatos.js";
---

<BaseLayout>
  <div class="max-w-2xl mx-auto">
    <div
      class="bg-[--bg-panel] border border-[--border] rounded-xl shadow-xl p-8 md:p-10"
    >
      <div class="chapter-title">Todos los Relatos</div>
      <ul class="doc-frag space-y-6 max-h-80 overflow-y-auto pr-2 pb-4">
        {
          relatos.map((relato) => (
            <li class="group">
              <a
                href={relato.url}
                class="block p-4 transition-all duration-300 bg-[--bg-main]/60 hover:bg-[--bg-main]/80 border border-[--border] hover:border-[--accent]/50 hover:shadow-lg"
              >
                <div class="flex items-center justify-between">
                  <div>
                    <h2 class="text-xl font-medium text-[--accent] group-hover:text-[--accent-strong] transition-colors">
                      {relato.titulo}
                    </h2>
                    <p class="text-sm text-[--text-muted] mt-1">
                      {relato.subtitulo}
                    </p>
                  </div>
                  <span class="text-[--accent-strong] opacity-0 group-hover:opacity-100 transition-opacity">
                    →
                  </span>
                </div>
              </a>
            </li>
          ))
        }
      </ul>
    </div>
  </div>
</BaseLayout>
